# First-party dependencies, DECLARED BY COMMIT (the replacement for git
# submodules). Bump a pin by editing the @<sha> and re-running
# scripts/bootstrap_env.sh. Transitive scientific deps (jax, equinox, optax,
# optuna, wandb, ...) come from these packages' own pyprojects.
#
# POLICY: never hand-edit installed copies (site-packages). A fix lands in its
# source repo, gets committed+pushed, the pin here is bumped, and the env is
# rebuilt/reinstalled. (The 2026-08-06 minergym adjacency fix was the last
# hand-patch ever.)

# --- morel: the formalism + algorithms (one repo, several distributions) ----
morel @ git+https://github.com/Terramorpha/morel@89c0484
morel-amorpheus @ git+https://github.com/Terramorpha/morel@89c0484#subdirectory=third_party/morel_amorpheus
morel-b2b @ git+https://github.com/Terramorpha/morel@89c0484#subdirectory=third_party/morel_b2b
morel-b2b-amorpheus @ git+https://github.com/Terramorpha/morel@89c0484#subdirectory=third_party/morel_b2b_amorpheus
morel-b2b-modumorph @ git+https://github.com/Terramorpha/morel@89c0484#subdirectory=third_party/morel_b2b_modumorph
morel-modumorph @ git+https://github.com/Terramorpha/morel@89c0484#subdirectory=third_party/morel_modumorph

# --- the benchmark --------------------------------------------------------
building2building @ git+https://github.com/vtaboga/Building2Building@698ca69

# --- the EnergyPlus wrapper (adjacency fix = 7520086) -----------------------
minergym @ git+https://github.com/Terramorpha/minergym@7520086

# --- URMA framework (only needed for morel_b2b_urma experiments) ------------
# rl-x @ git+https://github.com/nico-bohlinger/RL-X@70e220a
